# automated-obstacle-buzzer
This project uses an ultrasonic sensor with Arduino to measure the distance of nearby objects. When an object comes within a set range, the Arduino sends an alert to a computer via serial communication, and the computer generates a warning sound for user notification.
